apiVersion: v1
kind: Service
metadata:
  name: {{ include "app_service.fullname" . }}
  labels:
    {{- include "app_service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app_service.selectorLabels" . | nindent 4 }}
